# change_filenames
Go through all steps within the QuadraNiftiTool. 
Then place the files within the mr2pet_hires folder and run the Macro_visual_reads script in ImageJ. 
You can do this by clicking on new macro and pasting the script.
